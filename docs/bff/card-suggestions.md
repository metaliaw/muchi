[English](card-suggestions.md) · [Español](card-suggestions.es.md)

# Card Suggestions Correct a Name Prefix

`GET /api/card/suggestions` returns translated names beginning with the supplied prefix. It helps a person recover from a near match without searching every store.

The route is implemented by [`read_suggestions`](../../server/main.py).
