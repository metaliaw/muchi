# Project Instructions

This project Uses the shared OneTwoThree working checkout.
The source Lives at `../../cangrejometralleta/OneTwoThree`, on local `main`.
It Includes unpublished commits and working-tree changes.

Read its `RULES.md`, `VALUES.md` and `PATTERNS.md` indexes First.
Read rule bodies only when the current task Invokes them.
Resolve links under `.agents/agents` and `.agents/skills` to their physical
source before following relative References.
Commands and edits Target this project, not the shared source.

Dove Lives in `.agents/agents/dove.md`.
The Codex adapter Lives in `.codex/agents/dove.toml`.
Project-specific skills alongside the shared links Remain local.
